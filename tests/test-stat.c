#include <stdio.h>
#include <stdlib.h>

const char *names[] = {"blk1", "blk2", "blk3"};

int main(void) {
  // stat_init();

  // for (int j = 0; j < 10; j++) {
  //   for (int i = 0; i < 3; i++) {
  //     stat_record(names[i], i * 10);
  //   }
  // }

  // stat_show();
  // return 0;
}